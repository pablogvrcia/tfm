# CPU-only requirements (lighter, no CUDA)
# Install with: pip install -r requirements-cpu.txt

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# CLIP (lighter CPU version)
open-clip-torch>=2.20.0

# SAM 2 (Segment Anything Model 2)
git+https://github.com/facebookresearch/segment-anything-2.git

# Utilities
scikit-image>=0.21.0
matplotlib>=3.7.0
tqdm>=4.65.0

# Note: Stable Diffusion is heavy and slow on CPU
# For full inpainting functionality, use Docker or a GPU machine
